Add an OKF-native agentic capability benchmark for the ecosystem. The first round fixes Cline 3.0.60 and one OpenRouter model, runs a no-OKF baseline first, then evaluates one pinned rival at a time across all large deterministic tasks before advancing. Benchmark tasks, harness configuration, rival configuration and persisted run evidence are OKF concepts; generated runs record deterministic grading, wall time, observed tool invocation, token usage/cost when authoritatively exposed, and hashes/references for large outputs. The initial task set uses 1000+ record heterogeneous bundles and includes a 1200-record JSON-to-OKF conversion exercise.
