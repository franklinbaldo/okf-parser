---
type: Release
title: okf-parser 0.14.2
description: propose RFC 0005 for relational writes to frontmatter fields
---

# okf-parser 0.14.2

## RFC 0005: relational writes to frontmatter fields

Proposes `okf-parser apply PATH --type "Rotina" --sql "UPDATE concepts SET ... WHERE ..."`,
responding to the direction `duckdb` and `schema` never opened: mutating
frontmatter in bulk instead of only reading it.

- `--type` (an exact match against the authored `type` field) selects which
  concept type is materialized; `apply` never touches the rest of the
  bundle. The SQL table is always named `concepts` — an earlier draft named
  it by the type's slug, which review caught as broken: `type_slug()` isn't
  injective (this RFC's own `"Revisao Ciencia"` / `"Revisão Ciência"`
  example collides on one slug), so it can't reliably select exactly one
  type;
- runs one parsed `UPDATE`, and nothing else, against that temporary table
  (not a view — DuckDB cannot `UPDATE` a view), so plain `SET field = value`
  works without JSON-path expressions against `frontmatter_json`;
- one writable namespace per invocation (the selected type's authored
  frontmatter keys, exactly once) plus filesystem identity under a reserved
  `__okf_*` prefix, with an explicit guard against DuckDB's case-insensitive
  identifier rules, not just the prefix;
- v1 scope is top-level scalar fields with lexical `VARCHAR` semantics and an
  explicit presence/null contract (`SET field = NULL` deletes the key);
  structured (list/map) fields are rejected as mutation targets rather than
  silently mistyped;
- dry-run by default, matching `format`; a document that cannot round-trip
  losslessly aborts the whole `--write` invocation, not just that file;
- writes back through a purpose-built round-trip YAML loader instead of
  reserializing frontmatter wholesale, so an untouched key, comment or blank
  line survives;
- validates the candidate bundle against the *original* bundle's own
  diagnostics and aborts only on newly introduced normative ones, so `apply`
  stays usable on a bundle a migration is meant to repair;
- re-checks every path considered during validation immediately before
  writing — content hash for touched files, size/mtime for the hardlinked
  rest, plus a directory listing diff for additions/deletions — and aborts
  if anything changed since `apply` validated it; each file replace is an
  atomic rename, not an in-place write;
- bounds its idempotency guarantee to what `apply` itself controls: no diff
  from its own machinery beyond what the caller's SQL output specifies. A
  mutation that changes `type` (if allowed at all, per the open fork below)
  converges for free — the second run's `--type` selects zero rows once the
  first run has migrated them all.

Status remains `proposed`. Three rounds of PR review (#30) progressively
found: the mechanism unimplementable as first drafted; presence/null
semantics, concurrency, and validation baseline undefined after the
per-type-table redesign; and finally that naming the table by type slug
broke type selection, rename idempotency, and the `--field` sugar all at
once, fixed here by decoupling `--type` (exact) from the table name (always
`concepts`). One design fork is still genuinely open — whether a mutation
that changes `type` itself is allowed at all — and is what keeps this RFC at
`proposed` rather than `accepted`. #26 stays open until it resolves.
