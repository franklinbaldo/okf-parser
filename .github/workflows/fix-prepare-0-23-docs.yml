name: Fix 0.23.0 docs anchor

on:
  pull_request:
    types: [opened, synchronize]

permissions:
  contents: write

jobs:
  fix:
    if: github.event.pull_request.head.ref == 'feat/revision-digests-0.23.0'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.head.ref }}
      - name: Fix temporary docs anchor
        shell: bash
        run: |
          set -euo pipefail
          python scripts/fix_prepare_0_23_docs.py
          rm scripts/fix_prepare_0_23_docs.py
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add scripts/prepare_0_23.py scripts/fix_prepare_0_23_docs.py
          git commit -m "ci: fix inventory docs applicator anchor"
          git push origin "HEAD:${{ github.event.pull_request.head.ref }}"
